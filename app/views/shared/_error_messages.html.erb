<% if @bucketter.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains<%= pluralize(@bucketter.errors.count,"error")%>
    </div>
    <ul>
      <% @bucketter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
